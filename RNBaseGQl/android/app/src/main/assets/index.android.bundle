this.dbName = 'UserDatabase';
    this.db = SQLite.openDatabase({name : this.dbName, createFromLocation: 1},
      () => {
        console.log('DB created');
      },
      () => {
        console.log('error while creating DB')
      });